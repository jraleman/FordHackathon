import React from 'react';

class FilterItem extends React.Component {
  constructor (props) {
    super(props);
    this.state = {};
    return ;
  }
  render () {
    return (
      <React.Fragment>
        <p>{ "FilterItem" }</p>
      </React.Fragment>
    );
  }
}

export default FilterItem;

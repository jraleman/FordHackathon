import React from 'react';

class GridFilters extends React.Component {
  constructor (props) {
    super(props);
    this.state = {};
    return ;
  }
  render () {
    return (
      <React.Fragment>
        <p>{ "GridFilters" }</p>
      </React.Fragment>
    );
  }
}

export default GridFilters;
